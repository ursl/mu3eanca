# Copy to .env and adjust per machine (do not commit .env if it holds secrets).
# RelVal dashboard reads the same tree as Snakemake relval (mu3e_relval_basedir in prompt/relval/config.yaml).

RELVAL_BASEDIR=/mnt/data2/relval
