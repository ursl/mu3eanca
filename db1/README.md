# REST api to mongodb "community" server

`rest` contains the first working bare-bones version. Documented in its own README.md

## Test http into PSI
`read.js` used to test whether PSI blocks indeed everything (it does)

