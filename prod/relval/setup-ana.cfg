# Build local relval histogram tools (prod/ana: runFillHistograms, runCompareHistograms).
# Requires ROOT. Path: relval_code_basedir/ana (host overlay sets relval_code_basedir to this tree).
#
#   ./relval -c setup-ana.cfg
#   ./relval -c setup-ana.cfg -n

target: setup

ana_build: true
# ana_dir: "ana"

make_jobs: 10
