# all relval: run configured tasks for each included version config.
#
#   ./relval -c config-relval-all.cfg
#   ./relval -c config-relval-all.cfg -n
#
# Prerequisite: run setup for each MU3E version first, e.g.
#   ./relval -c config-setup-all.cfg

target: all

include: config-relval-v6.5.cfg
include: config-relval-v6.9.cfg
include: config-relval-v7.0pre0.cfg
