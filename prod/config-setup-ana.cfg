# Build local relval histogram tools (prod/ana: runFillHistograms, runCompareHistograms).
# Requires ROOT. Path: relval_code_basedir/ana (from host overlay).
#
#   ./relval -c config-setup-ana.cfg
#   ./relval -c config-setup-ana.cfg -n

target: setup

ana_build: true
# ana_dir: "ana"

make_jobs: 10
